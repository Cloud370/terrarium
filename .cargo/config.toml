# Cross-compilation for Windows release binaries: use the mingw-w64 linker
# (`apt install mingw-w64`) when building with --target x86_64-pc-windows-gnu.
[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
